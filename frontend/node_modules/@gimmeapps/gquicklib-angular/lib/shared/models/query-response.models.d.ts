import { BaseModel } from "./abstract.models";
export interface Page<T extends BaseModel> {
    current_page: number;
    data: T[];
    from: number;
    per_page: number;
    to: number;
    total: number;
}
